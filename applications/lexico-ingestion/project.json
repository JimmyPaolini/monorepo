{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "lexico-ingestion",
  "projectType": "application",
  "sourceRoot": "applications/lexico-ingestion",
  "tags": [
    "framework:nest-commander",
    "framework:nestjs",
    "generator:nestjs-command-application",
    "language:typescript"
  ],
  "targets": {
    "analyze-code": {},
    "clean": {},
    "clear": {
      "description": "Clear ingested data from the database",
      "executor": "nx:run-commands",
      "options": {
        "command": "node --import @swc-node/register/esm-register src/main.ts clear",
        "cwd": "applications/lexico-ingestion"
      }
    },
    "dependency-cruiser": {},
    "dictionary": {
      "description": "Ingest dictionary entries into the database",
      "executor": "nx:run-commands",
      "options": {
        "command": "node --import @swc-node/register/esm-register src/main.ts dictionary",
        "cwd": "applications/lexico-ingestion"
      }
    },
    "eslint": {},
    "format": {},
    "knip": {},
    "lint": {},
    "manual": {
      "description": "Ingest manually curated entries into the database",
      "executor": "nx:run-commands",
      "options": {
        "command": "node --import @swc-node/register/esm-register src/main.ts manual",
        "cwd": "applications/lexico-ingestion"
      }
    },
    "markdown-lint": {},
    "oxfmt": {},
    "oxlint": {},
    "prettier": {},
    "repl": {
      "description": "Start the LexicoIngestion application in REPL mode",
      "executor": "nx:run-commands",
      "options": {
        "command": "node --import @swc-node/register/esm-register src/repl.ts",
        "cwd": "${projectRoot}"
      }
    },
    "spell-check": {},
    "start": {
      "description": "Start the LexicoIngestion application",
      "executor": "nx:run-commands",
      "options": {
        "command": "node --import @swc-node/register/esm-register src/main.ts lexico-ingestion",
        "cwd": "${projectRoot}"
      }
    },
    "test": {
      "configurations": {
        "coverage": {
          "command": "vitest run --coverage"
        },
        "end-to-end": {
          "command": "vitest run end-to-end.test.ts"
        },
        "integration": {
          "command": "vitest run integration.test.ts"
        },
        "unit": {
          "command": "vitest run unit.test.ts"
        },
        "watch": {
          "command": "vitest"
        }
      },
      "description": "Run all tests",
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "${projectRoot}"
      }
    },
    "type-coverage": {},
    "typecheck": {},
    "wiktionary": {
      "description": "Ingest Wiktionary data into the database",
      "executor": "nx:run-commands",
      "options": {
        "command": "node --import @swc-node/register/esm-register src/main.ts wiktionary",
        "cwd": "applications/lexico-ingestion"
      }
    },
    "yaml-lint": {}
  }
}
