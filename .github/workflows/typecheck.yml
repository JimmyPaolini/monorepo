name: 🏷️ Typecheck

on:
  push:
    branches:
      - main
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  typecheck:
    name: 🏷️ Typecheck
    runs-on: ubuntu-latest

    steps:
      - name: 🔧 Setup Nx workspace
        uses: ./.github/actions/nx-setup

      - name: 🏷️ Typecheck
        run: npx nx affected -t typecheck --parallel=3 --verbose
