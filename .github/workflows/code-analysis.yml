name: 🧑‍💻 Code Analysis

on:
  push:
    branches:
      - main
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  code-analysis:
    name: ${{ matrix.check.name }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        check:
          - name: 🏷️ Typescript Check
            run: pnpm exec nx affected -t typecheck --parallel=3
            upload_artifacts: false

          - name: 🧹 ESLint Check
            run: pnpm exec nx affected -t lint --parallel=3
            upload_artifacts: false

          - name: 🖼️ Markdown Lint
            run: pnpm exec nx affected -t markdown-lint --parallel=3
            upload_artifacts: false

          - name: 📄 YAML Lint
            run: pnpm exec nx affected -t yaml-lint --parallel=3
            upload_artifacts: false

          - name: 🎨 Format Check
            run: pnpm exec nx affected -t format --configuration=check --parallel=3
            upload_artifacts: false

          - name: ✂️ Knip Check
            run: pnpm exec nx affected -t knip --parallel=3
            upload_artifacts: false

          - name: 🧙‍♂️ Spell Check
            run: pnpm exec nx affected -t spell-check --parallel=3
            upload_artifacts: false

          - name: 🎨 Stylelint
            run: pnpm exec nx affected -t stylelint --parallel=3
            upload_artifacts: false

          - name: 🔖 Type Coverage
            run: pnpm exec nx affected -t type-coverage --parallel=3
            upload_artifacts: true

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🕋 Setup Monorepo
        uses: ./.github/actions/setup-monorepo

      - name: ${{ matrix.check.name }}
        run: ${{ matrix.check.run }}

      - name: 📊 Upload Type Coverage
        if: matrix.check.upload_artifacts && always()
        uses: actions/upload-artifact@v4
        with:
          name: type-coverage-reports
          path: |
            applications/**/type-coverage-report.txt
            packages/**/type-coverage-report.txt
            tools/**/type-coverage-report.txt
          retention-days: 30
