name: 🎨 Format Check

on:
  push:
    branches:
      - main
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  format:
    name: 🎨 Format Check
    runs-on: ubuntu-latest

    steps:
      - name: � Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: �🔧 Setup Nx workspace
        uses: ./.github/actions/nx-setup

      - name: 🎨 Format Check
        run: npx nx affected -t format --configuration=check --parallel=3 --verbose
